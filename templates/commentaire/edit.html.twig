{% extends 'baselog.html.twig' %}

{% block title %}Edit Commentaire{% endblock %}

{% block body %}
<br><br><br><br>
    <div class="container mt-5">
       

        <!-- Form Section -->
        <div class="row">
            <div class="col-lg-8 offset-lg-2 col-md-10 offset-md-1">
                <div class="card shadow-sm border-light rounded">
                    <div class="card-body">
                        <!-- Include the form template -->
                        {{ include('commentaire/_form.html.twig', {'button_label': 'Update', 'class': 'btn btn-primary btn-lg w-100'}) }}
                    </div>
                </div>
            </div>
        </div>

        <!-- Back Link Section -->
        <div class="row mt-4">
            <div class="col-12 text-center">
                <a href="{{ path('app_commentaire_index') }}" class="btn btn-secondary btn-sm">Back to List</a>
            </div>
        </div>

        <!-- Delete Form Section -->
        <div class="row mt-4">
            <div class="col-12 text-center">
                {{ include('commentaire/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
