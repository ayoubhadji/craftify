<form method="post" action="{{ path('app_expedition_delete', {'id': expedition.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ expedition.id) }}">
    <button class="btn">Delete</button>
</form>
