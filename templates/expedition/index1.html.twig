{% extends 'baselog.html.twig' %}

{% block title %}📦 Gestion des Expéditions{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h2 class="text-center text-primary fw-bold mb-4">📦 Liste des Expéditions</h2>

        {% if expeditions is empty %}
            <div class="alert alert-warning text-center fw-bold fs-4">
                🚀 Aucune expédition disponible pour le moment !
            </div>
        {% else %}
            <div class="table-responsive">
                <table class="table table-bordered table-hover text-center align-middle shadow-sm">
                    <thead class="table-primary">
                        <tr>
                           
                            <th>📛 Nom de l'expédition</th>
                            <th>🔎 Détails</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for expedition in expeditions %}
                        <tr>
                        
                        <td>{{ expedition.titre }}</td>
                       <td>
                <a href="{{ path('app_expedition_show1', { 'id': expedition.id }) }}" class="btn btn-info btn-sm">
                    Voir Détails
                </a>
            </td>
                        
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        {% endif %}
    </div>
{% endblock %}
