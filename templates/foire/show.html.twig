{% extends 'base2.html.twig' %}

{% block title %}Foire{% endblock %}

{% block body %}
    <h1>Foire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ foire.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ foire.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ foire.description }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ foire.dateDebut ? foire.dateDebut|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ foire.dateFin ? foire.dateFin|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ foire.lieu }}</td>
            </tr>
            <tr>
                <th>Capacite_max</th>
                <td>{{ foire.capaciteMax }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ foire.createdAt ? foire.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ foire.prix }}</td>
            </tr>
            <tr>
                <th>Rate</th>
                <td>{{ foire.rate }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_foire_index') }}">back to list</a>

    <a href="{{ path('app_foire_edit', {'id': foire.id}) }}">edit</a>

    {{ include('foire/_delete_form.html.twig') }}
{% endblock %}
