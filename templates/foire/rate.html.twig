{# templates/foire/rate.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Rate this Foire{% endblock %}

{% block body %}
    <h1>Rate the Foire: {{ foire.nom }}</h1>

    {{ form_start(form) }}
        {{ form_row(form.rate) }}
        <button type="submit">Submit Rating</button>
    {{ form_end(form) }}
{% endblock %}