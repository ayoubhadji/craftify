<!DOCTYPE html>

<title>SliderItem</title>

{% block body %}
    <h1>SliderItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ slider_item.id }}</td>
            </tr>
            <tr>
                <th>ImagePath</th>
                <td>{{ slider_item.imagePath }}</td>
            </tr>
            <tr>
                <th>AltText</th>
                <td>{{ slider_item.altText }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ slider_item.url }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ slider_item.position }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_slider_item_index') }}">back to list</a>

    <a href="{{ path('app_slider_item_edit', {'id': slider_item.id}) }}">edit</a>

    {{ include('slider_item/_delete_form.html.twig') }}
{% endblock %}
